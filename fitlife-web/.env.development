# Vite dev server proxies /api to this backend
# Default /api proxy is configured in vite.config.ts
# Only set VITE_API_URL if you need to override the proxy

# VITE_API_URL=http://localhost:5269/api

VITE_APP_NAME=FitLife
