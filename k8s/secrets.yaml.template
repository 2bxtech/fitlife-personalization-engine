apiVersion: v1
kind: Secret
metadata:
  name: fitlife-secrets
  namespace: fitlife
type: Opaque
stringData:
  # ⚠️ TEMPLATE: Copy this file to secrets.yaml and replace placeholder values.
  # NEVER commit secrets.yaml — it is in .gitignore.
  SQL_CONNECTION_STRING: "<REPLACE_WITH_SQL_CONNECTION_STRING>"
  JWT_SECRET: "<REPLACE_WITH_256_BIT_SECRET_KEY>"
  REDIS_PASSWORD: "<REPLACE_WITH_REDIS_PASSWORD>"
